void f(double current, int i, int n) {
    if (i > n) {
        printf("%.2f", current);
        return;
    }
    double new = sqrt(current + i);
    f(new, i + 1, n);
}

int fib(int i){
    if(i=0) return 0;
    if(i=1) return 1;} //or i=1,i=2...

