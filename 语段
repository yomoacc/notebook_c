需明确结构体变量与结构体指针访问成员的语法规则：
结构体变量用 . 访问成员；
结构体指针用 -> 访问成员，或先解引用指针（*p）后用 . 访问。
struct S a;        // 定义结构体变量a（占一块内存，有自己的地址：&a）
struct S *p = &a;  // p是「结构体指针」，存储的是a的地址（p = &a）
a是结构体变量，不是指针，不能使用 -> 运算符。应改为 a.x
//
%d是用来输出int类型的格式符，而sqrt(n)是double类型，两者不兼容 —— 这是 C 语言的严格语法要求，格式符必须和数据类型一一对应。
